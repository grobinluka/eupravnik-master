<%= simple_form_for([@discussion, @discussion.replies.build]) do |f| %> <!-- tukaj dobimo discussion id, dobimo discussion reply build won't "create" a record in database, 
just create a new object in memory so that the view can take this object and display something, especially for a form. -->

<div class="field">
  <div class="control">
    <%= f.input :reply_post, input_html: { class: 'textarea'}, wrapper: false, label_html: { class: 'label' } %>
  </div>
</div>

<%= f.button :submit, 'Leave a reply', class:'button is-info' %>

<% end %>